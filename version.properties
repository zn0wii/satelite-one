# 全局唯一的版本定义处 — 所有版本相关的地方都读这里:
#   app/build.gradle.kts (versionCode / versionName / BuildConfig)
#   设置页"版本"行 (BuildConfig.VERSION_NAME)
#   CI (校验 tag = v<versionName>)
# 发版步骤: 改这里 → 提交 → git tag v<versionName> → git push origin v<versionName>
versionCode=11
versionName=0.5.5
